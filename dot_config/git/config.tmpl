[user]
name = {{ .git.name }}
email = {{ .git.email }}

{{ if hasKey .git "signingKey" -}}
[user]
signingKey = {{ .git.signingKey }}
[commit]
gpgSign = true
[tag]
gpgSign = true
{{- end }}

[sequence]
editor = interactive-rebase-tool

[difftool]
prompt = false

{{ if lookPath "ec" -}}
[merge]
tool = ec

[mergetool "ec"]
cmd = ec "$BASE" "$LOCAL" "$REMOTE" "$MERGED"
trustExitCode = true
{{- end }}

[alias]
br = !broot --conf '{{ joinPath (env "XDG_CONFIG_HOME") "broot" "git.conf.toml" }}'
ccommit = -c 'core.editor=meteor --as-git-editor' commit
graph = !serie
unstage = restore --staged

[advice]
defaultBranchName = false

[include]
path = {{ joinPath (env "XDG_CONFIG_HOME") "delta" "delta.gitconfig" }}
