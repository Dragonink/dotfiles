# Source .profile
if [ -r "$HOME/.profile" ]; then
	#shellcheck disable=SC1091
	source "$HOME/.profile"
fi

# Source .bashrc
if [ -r "$HOME/.bashrc" ]; then
	#shellcheck disable=SC1091
	source "$HOME/.bashrc"
fi
