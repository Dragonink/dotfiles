{{- if not (isExecutable "/usr/bin/broot") }}
df
du
{{- end }}
{{- if not (isExecutable "/usr/bin/vendor_perl/mimeopen") }}
mimeopen
{{- end }}
{{- if not (isExecutable "/usr/bin/xdg-open") }}
xdg-open
{{- end}}
